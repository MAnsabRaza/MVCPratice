@model MVC_Pratice.Models.User
@{
    ViewBag.Title = "Register";
}

<div class="min-h-screen flex items-center justify-center">
    <div class="bg-white shadow-2xl rounded-xl p-8 w-full max-w-lg">
        <h2 class="text-3xl font-bold text-center bg-gradient-to-br from-gray-800 to-black bg-clip-text text-transparent mb-3">Create Your Account</h2>

        @using (Html.BeginForm("Register", "Auth", FormMethod.Post))
        {
            @Html.AntiForgeryToken()

            <div class="mb-2">
                <label class="block text-gray-700 font-semibold mb-1">Full Name</label>
                <div class="relative">
                    <span class="absolute left-3 top-3 text-gray-400">
                        <i class="fas fa-user"></i>
                    </span>
                    @Html.TextBoxFor(m => m.name, new
                    {
                        @class = "pl-10 pr-3 py-2 w-full border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none",
                        placeholder = "Enter your full name",
                        required = "required"
                    })
                </div>
            </div>

            <div class="mb-2">
                <label class="block text-gray-700 font-semibold mb-1">Phone Number</label>
                <div class="relative">
                    <span class="absolute left-3 top-3 text-gray-400">
                        <i class="fas fa-phone"></i>
                    </span>
                    @Html.TextBoxFor(m => m.phone_number, new
                    {
                        @class = "pl-10 pr-3 py-2 w-full border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none",
                        placeholder = "Enter your phone number",
                        required = "required"
                    })
                </div>
            </div>

            <div class="mb-2">
                <label class="block text-gray-700 font-semibold mb-1">Address</label>
                <div class="relative">
                    <span class="absolute left-3 top-3 text-gray-400">
                        <i class="fas fa-map-marker-alt"></i>
                    </span>
                    @Html.TextAreaFor(m => m.address, new
                    {
                        @class = "pl-10 pr-3 py-2 w-full border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none",
                        placeholder = "Enter your address",
                        rows = "3",
                        required = "required"
                    })
                </div>
            </div>

            <div class="mb-2">
                <label class="block text-gray-700 font-semibold mb-1">Email</label>
                <div class="relative">
                    <span class="absolute left-3 top-3 text-gray-400">
                        <i class="fas fa-envelope"></i>
                    </span>
                    @Html.TextBoxFor(m => m.email, new
                    {
                        @type = "email",
                        @class = "pl-10 pr-3 py-2 w-full border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none",
                        placeholder = "Enter your email",
                        required = "required"
                    })
                </div>
            </div>

            <div class="mb-2">
                <label class="block text-gray-700 font-semibold mb-1">Password</label>
                <div class="relative">
                    <span class="absolute left-3 top-3 text-gray-400">
                        <i class="fas fa-lock"></i>
                    </span>
                    @Html.PasswordFor(m => m.password, new
                    {
                        @class = "pl-10 pr-3 py-2 w-full border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none",
                        placeholder = "Enter your password",
                        required = "required"
                    })
                </div>
            </div>

            <button type="submit"
                    class="w-full py-3 text-lg text-white
bg-gradient-to-r from-gray-800 to-black
hover:from-black hover:to-gray-800
rounded-lg shadow-lg transition duration-300">
                Sign Up
            </button>

            <p class="text-center text-gray-600 mt-2">
                Already have an account?
                <a href="/Auth/Login" class="bg-gradient-to-br from-blue-500 to-gray-800 bg-clip-text text-transparent
                   hover:underline">Login here</a>
            </p>
        }
    </div>
</div>

